Private photo inbox.

Put original photos here, then run:

```bash
npm run dainagon:photos
```

Converted WebP files are written to `public/assets/prelude-cache/`.
Duplicate originals are removed by content hash, and successfully converted originals are deleted.
